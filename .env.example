# Copy to .env (gitignored) and fill in. All core market data is public/unauthenticated.

# Anthropic API key — used ONLY by the matching-layer LLM adjudicator (plan §6, milestone 6).
ANTHROPIC_API_KEY=

# Telegram alerting (plan §8, milestone 9).
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# Deliberately absent: wallets, USDC, Polygon RPC keys, Kalshi trading keys.
# This project is a detector only (plan §14) — it must never hold trading credentials.
