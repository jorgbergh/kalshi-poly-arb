"""SQLite store (Milestone 8, plan §9.8, §11).

STUB. Will create and manage the tables (all carrying ``schema_version``):
markets, pairs, verdicts, opportunities, drops, cycles, stage_stats — plus
the predefined views loaded from views.sql. Append-only ledgers; additive
migrations only (plan §9.10). See STATE_SCHEMA.md for the registry.
"""
