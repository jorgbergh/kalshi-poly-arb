"""Telegram alerter (Milestone 9, plan §8, §11).

STUB. Will send alerts (bot token via TELEGRAM_BOT_TOKEN env var) including
both titles, direction, fill prices, per-leg fees, net-per-pair, ROI%,
achievable size, and the LLM resolution_caveats — so a human sees the risk
before acting. De-dup: alert on appearance and material change only.
"""
